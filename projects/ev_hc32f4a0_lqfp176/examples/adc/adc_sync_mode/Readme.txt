================================================================================
                                样例使用说明
================================================================================
Date            Author      IAR       MDK       GCC
2022-03-31      CDT         7.70      5.36      gcc-arm-none-eabi-10.3-2021.10-win32

================================================================================
平台说明
================================================================================
GCC工程，由Eclipse IDE外挂GNU-ARM Toolchain，再结合pyOCD GDB Server实现工程的编译、
链接和调试。在用Eclipse导入工程后，请将xxxx_PyOCDDebug中pyocd-gdbserver和SVD文件
设置为正确的路径；请将xxxx_PyOCDDownload中pyocd设置为正确的路径。注意，这些路径不
能包含非英文字符。

================================================================================
功能描述
================================================================================
本例程使用了全部的ADC单元来演示ADC的协同模式，并将各ADC的扫描时序通过TIMERA的PWM输出，
可通过示波器观察。

说明：
工作在协同模式的ADC单元：
1. 只使用序列A，禁止序列B；
2. 禁止同一引脚在不同单元同时被采样；
3. 应使用相同的初始化配置；
4. 序列A使能相同的通道数；单元之间，序列A使能的通道可以不相同；
5. 各通道的采样时间应相同；
6. 例程中当ADC1和ADC2协同工作(APP_SYNC_UNITS == ADC_SYNC_ADC1_ADC2)，且协同模式为循环
   延迟触发模式时(ADC_SYNC_MD == ADC_SYNC_SINGLE_DELAY_TRIG)，只使能了一个ADC通道，
   且其对应的是同一个外部模拟输入引脚，这样配置的目的是为了实现对同一个模拟输入引脚以
   5Msps的速率进行采样。

================================================================================
测试环境
================================================================================
测试用板:
---------------------
EV_F4A0_LQ176_Rev1.0

辅助工具:
---------------------
示波器

辅助软件:
---------------------

================================================================================
使用步骤
================================================================================
1）将电位器旁的J13开关拨为AVCC<->VR；
2）将 PE7 与 PA0（连着按键 K10）短接；
3）例程默认 ADC1 和 ADC2 协同工作。将示波器探头连接至 PE11（指示 ADC1 采样时序），
   PB10（指示 ADC2 采样时序）；
4）打开工程，重新编译，启动调试或直接下载程序运行；
5）按下按键 K10，触发各 ADC 扫描，示波器显示两路频率为 1.25MHz 的 PWM；
6）增加一路示波器，将其探头连接至 PE15（ADC3 采样时序）；
7）将宏 ADC_SYNC_UNITS 定义为 ADC_SYNC_ADC1_ADC2_ADC3，使 ADC1、ADC2 和 ADC3 协同
   工作。重新编译工程，启动调试或直接下载程序运行，按下按键 K10，可看到示波器显示 3 路
   频率为 416.667KHz 的 PWM。

================================================================================
注意
================================================================================
无

================================================================================
